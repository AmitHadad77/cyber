'use client';
import { FormEvent, useState } from 'react';
export default function Contact(){const [sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const body=encodeURIComponent(`שם: ${fd.get('name')}
אימייל: ${fd.get('email')}
חברה: ${fd.get('company')}
נושא: ${fd.get('topic')}`);location.href=`mailto:demo@aether.example?subject=${encodeURIComponent('בקשת הדגמה — AI Aether')}&body=${body}`;setSent(true)}return <section className="contact" id="contact"><div className="container contactLayout"><div className="sectionHead"><span>PRIVATE DEMO</span><h2>ראו את המוצר דרך תרחיש האבטחה שלכם.</h2><p>טופס הדגמה ללא שמירת מידע. השליחה פותחת הודעת אימייל מסודרת.</p></div><form onSubmit={submit} className="contactCard">{sent?<div className="success"><i>✓</i><h3>הבקשה מוכנה</h3><p>נפתחה הודעת אימייל עם הפרטים.</p></div>:<><div className="formRow"><label>שם מלא<input name="name" required autoComplete="name"/></label><label>אימייל עסקי<input name="email" type="email" required autoComplete="email"/></label></div><label>חברה<input name="company" required autoComplete="organization"/></label><label>מה תרצו לראות?<textarea name="topic" placeholder="ניהול זהויות, תצורה שגויה או תגובה לאירועים"/></label><button className="button primary">הכנת בקשת הדגמה</button><small>הטופס אינו שומר מידע באתר.</small></>}</form></div></section>}
